{{#> PageContainer className='centered'}}
    <div class="{{ type }}-wrapper">
        <div class="{{ type }}-wrapper__title">
            <h1 class="{{ type }}-wrapper__title-text">{{ capitalize type }}</h1>
        </div>
        {{> @partial-block }}
    </div>
{{/ PageContainer }}
